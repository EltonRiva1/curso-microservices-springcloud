spring:
  application:
    name: eurekaserver
  cache:
    type: caffeine
  security:
    basic:
      enabled: true
    user:
      name: eureka-user
      password: u-areskueer
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
